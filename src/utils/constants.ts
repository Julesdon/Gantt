// Constants for Gantt chart
export const ROW_HEIGHT = 36; // px per task row
export const COL_WIDTH = 32; // px per day column
export const WINDOW_DAYS = 60; // fixed window size
export const LEFT_COL_WIDTH = 280; // px (task name column)