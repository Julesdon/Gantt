export declare enum ItemPosition {
    PRE = "pre",
    MID = "mid",
    POST = "post"
}
export declare type Metadata = {
    meta: {
        [key: number]: {
            size: number;
            offset: number;
            localOffset: number;
        };
    };
    pre: {
        size: number;
        range: number[];
    };
    mid: {
        size: number;
        range: number[];
    };
    post: {
        size: number;
        range: number[];
    };
    total: {
        size: number;
        count: number;
    };
};
declare function useMetadata(count: number, size: number | Function, preCount: number, postCount: number): Metadata;
declare function useMetadataFixed(metadata: Metadata, clientSize: number, fillerType: any, minVisibleScrollViewWidth: number): Metadata;
export default useMetadata;
export { useMetadataFixed };
